import 'package:flutter/material.dart';
import '../../widgets/placeholder_scaffold.dart';

class ProgressionListScreen extends StatelessWidget {
  const ProgressionListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScaffold(title: 'Progression Detail Screen');
  }
}
